<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
		http://www.springframework.org/schema/security
		http://www.springframework.org/schema/security/spring-security-4.1.xsd">


    <beans:import resource="dao.xml"/>

    <http auto-config="true" use-expressions="true" authentication-manager-ref="jdbc_auth">
        <intercept-url pattern="/newsList" access="hasRole('ROLE_ADMIN')"/>

        <access-denied-handler error-page="/client?command=getNewsList" />
        <form-login login-page="/signIn"
                             authentication-failure-url="/authorList"
                             username-parameter="username"
                             password-parameter="password"
                             default-target-url="/newsList"/>
        <logout logout-success-url="/client?command=getNewsList"/>
        <csrf disabled="true"/>
    </http>

    <authentication-manager id="in-memory-auth">
        <authentication-provider>
            <user-service>
                <user name="admin" password="admin" authorities="ROLE_ADMIN" />
            </user-service>
        </authentication-provider>
    </authentication-manager>

    <authentication-manager id="jdbc_auth">
        <authentication-provider>
            <jdbc-user-service data-source-ref="dataSource"
                                        users-by-username-query="SELECT LOGIN AS USERNAME, PASSWORD, ENABLED FROM USERS WHERE LOGIN = ?"
                                        authorities-by-username-query="SELECT LOGIN AS USERNAME, ROLE_NAME AS ROLE FROM USERS U INNER JOIN ROLE R ON U.USER_ID = R.USER_ID WHERE LOGIN=?"/>
        </authentication-provider>
    </authentication-manager>
</beans:beans>